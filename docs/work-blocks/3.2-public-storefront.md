# Arbeitsblock 3.2 – Öffentliche Storefront und Speisekarte

## Freigabe und Referenz

1. Referenz: `main`, Commit `dcbe18e7ac7fc2999089c61d7d95c57bf37fdc14`.
2. Arbeitsblock 3.1 ist laut verbindlicher Übergabe abgeschlossen und formal freigegeben.
3. Nutzerfreigabe am 14. September 2026: „Arbeitsblock 3.2 ist mit dem vorgeschlagenen umfang formal
   freigegeben“. Dies ist die Umfangs-/Umsetzungsfreigabe, keine technische Endabnahme.
4. Lokaler Arbeitsbranch: `codex/ab3.2-public-storefront`.
5. Status: in PR #1 veröffentlicht und technisch geprüft; Pflicht-Gates `check` und `database` grün,
   technische Endfreigabe und Merge ausstehend.
6. Dieser Nachtrag dokumentiert die Umsetzung und ersetzt nicht die externe Architektur-SoT A2 oder
   das Projektprotokoll V2.

## Gelieferter Funktionsschnitt

| Paket              | Ergebnis                                                                                           |
| ------------------ | -------------------------------------------------------------------------------------------------- |
| Contracts          | Öffentliche DTOs, Eingabeprüfung und explizite Ausgabe-Allowlist                                   |
| Datenbank          | Zwei nur serverseitig ausführbare, rein lesende Funktionen; bestehende Resolver wiederverwendet    |
| API                | Zwei öffentliche GET-Endpunkte, parametrisierte Abfragen, Transaktionsrolle, Zeit-/Payload-Grenzen |
| Storefront         | Standortseite, Kategorien, Gerichte, Preise, Artikelstatus, Bestellart und Wunschterminprüfung     |
| Synthetische Daten | Wiederverwendbare SQL-Fixture und dazu passender JSON-Vertrag                                      |
| CI                 | Zusätzlicher echter API-/PostgreSQL-Treiberintegrationstest nach pgTAP im Job `database`           |
| Dokumentation      | ADR 0019, API-/Storefront-README, Umgebungs- und Storefront-Runbook                                |

Keine Bestellübermittlung, Zahlungen, personenbezogenen Daten, Deployment- oder Änderungen am
getrennten Asian-Kitchen-Projekt wurden ausgeführt.

## Nachweise und Grenzen

1. `pnpm check`: lokal bestanden. Formatierung, Linting, Typprüfung, 54 Tests und beide Builds sind
   grün. Der datenbankabhängige Test ist im Standardlauf bewusst übersprungen und läuft im
   gesonderten CI-Datenbankjob mit expliziter `TEST_DATABASE_URL`.
2. Neue SQL-Prüfungen: 38 von 38 in einer ergänzenden PGlite-0.5.8-/PostgreSQL-18.3-Laufzeit
   bestanden. Alle 18 Migrationen wurden dafür unverändert angewandt. Die Laufzeit enthält
   ausschließlich synthetische Daten und eine minimale Nachbildung des Supabase-Auth-Schemas.
3. API-/Treiberintegration: separat bestanden über `pg` und den tatsächlichen Worker-Handler gegen
   dieselbe lokale PostgreSQL-WASM-Laufzeit über einen Loopback-Socket.
4. Diese Ergänzungsnachweise ersetzen den verpflichtenden Supabase-/PostgreSQL-15-Lauf nicht. Docker
   und damit die reguläre Supabase-Testumgebung sind in dieser Sitzung nicht verfügbar.
5. Die Browserprüfung wird mit dem gebauten Anwendungscode im lokalen vinext-/workerd-Devserver und
   synthetischen HTTP-Antworten durchgeführt. Sie ersetzt keinen externen Preview-Rollout.
6. Die tatsächliche Hyperdrive-Konfiguration einschließlich Cache und Laufzeitrechten muss vor einem
   ausdrücklich freigegebenen Preview-Rollout geprüft werden.

## GitHub-Nachweis

1. Pull Request: `#1`, `codex/ab3.2-public-storefront` gegen `main`.
2. Head-Commit: `8b55e14fe6ce720dc0649237867b3b70ce9821ff`.
3. Ausgangscommit: `dcbe18e7ac7fc2999089c61d7d95c57bf37fdc14`.
4. GitHub meldet den Pull Request als konfliktfrei und zusammenführbar.
5. Pflichtprüfung `check`: am 15. September 2026 erfolgreich abgeschlossen.
6. Pflichtprüfung `database`: am 15. September 2026 erfolgreich abgeschlossen.

Der Pull Request wurde noch nicht zusammengeführt und es wurde kein Deployment ausgeführt.

## Nächster Schritt

Dokumentationskorrektur in PR #1 veröffentlichen und die Pflichtprüfungen erneut bis Grün verfolgen.
Danach die technische Endfreigabe und ausdrückliche Merge-Freigabe einholen. Nach dem Merge den
Gesamtfortschritt gemäß verbindlicher Abschlussregel ausweisen.
